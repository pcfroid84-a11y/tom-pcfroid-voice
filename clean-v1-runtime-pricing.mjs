import { CLEAN_V1_ROBUST_PATCHES } from "./clean-v1-runtime-robust.mjs";

function patch(search, replacement, label) {
  return `replaceOnce(${JSON.stringify(search)}, ${JSON.stringify(replacement)}, ${JSON.stringify(label)});`;
}

const patches = [];

patches.push(
  patch(
    `} from "./transcription-guard.mjs";\n\nconst PCFROID_KNOWLEDGE_CONTEXT`,
    `} from "./transcription-guard.mjs";\nimport {\n  buildControlledTariffSentence,\n  buildUncertainTariffSentence,\n  extractIndoorUnitCount,\n  getClimMaintenanceQuote,\n  inferClimMaintenanceConfig,\n  isClimMaintenanceTariffRequest,\n} from "./tariff-engine.mjs";\n\nconst PCFROID_KNOWLEDGE_CONTEXT`,
    "imports moteur tarifaire contrôlé",
  ),
);

patches.push(
  patch(
    `    lastTranscriptionAvgLogprob: null,\n    identityKnown: false,`,
    `    lastTranscriptionAvgLogprob: null,\n    tariffFlowMode: null,\n    tariffUnitCount: null,\n    tariffReturnStage: null,\n    tariffRecognitionFailures: 0,\n    identityKnown: false,`,
    "état temporaire du parcours tarifaire",
  ),
);

patches.push(
  patch(
    `          const cleanAdministrativeStages = new Set(["customer_status", "identity", "city", "address", "callback", "callback_number", "qualification"]);`,
    `          const tariffRequestNow =\n            state.serviceIntent === "entretien" &&\n            state.explicitEquipment === "climatisation" &&\n            isClimMaintenanceTariffRequest(callerMessage);\n\n          if (state.tariffFlowMode || tariffRequestNow) {\n            const returnStage = state.tariffReturnStage || flowStageAtTurnStart;\n            if (!state.tariffReturnStage) state.tariffReturnStage = returnStage;\n\n            const clearTariffFlow = () => {\n              state.tariffFlowMode = null;\n              state.tariffUnitCount = null;\n              state.tariffRecognitionFailures = 0;\n              state.tariffReturnStage = null;\n            };\n\n            const sendTariffResult = (quote) => {\n              const sentence = quote ? buildControlledTariffSentence(quote) : buildUncertainTariffSentence();\n              let continuation = null;\n              let resumeStage = returnStage;\n\n              if (resumeStage === "need") {\n                setFlowStage("customer_status", "tarif entretien répondu, reprise du parcours");\n                resumeStage = "customer_status";\n              } else if (resumeStage === "final_question") {\n                setFlowStage("final_followup", "tarif répondu après la question finale");\n                resumeStage = "final_followup";\n              }\n\n              if (["customer_status", "identity", "city", "address", "callback", "callback_number"].includes(resumeStage)) {\n                continuation = pendingQuestionForStage(resumeStage, state);\n              }\n\n              clearTariffFlow();\n              app.log.info(\n                { quoted: Boolean(quote), resumeStage, priceTtc: quote?.priceTtc || null },\n                quote ? "Tarif entretien clim annoncé avec validation PC Froid" : "Tarif incertain laissé à la validation PC Froid",\n              );\n\n              const exactSpeech = continuation ? sentence + " " + continuation : sentence;\n              sendToOpenAI({\n                type: "response.create",\n                response: {\n                  output_modalities: ["audio"],\n                  instructions: 'Dites exactement et uniquement : "' + exactSpeech.replace(/"/g, "'") + '" Aucun mot avant, aucun mot après.',\n                },\n              });\n            };\n\n            if (!state.tariffFlowMode) {\n              let inferredConfig = null;\n              for (const message of state.callerMessages || []) {\n                const candidateConfig = inferClimMaintenanceConfig(message);\n                if (candidateConfig) inferredConfig = candidateConfig;\n              }\n\n              const directQuote = inferredConfig ? getClimMaintenanceQuote(inferredConfig) : null;\n              if (directQuote) {\n                sendTariffResult(directQuote);\n                return;\n              }\n\n              state.tariffFlowMode = "count";\n              state.tariffRecognitionFailures = 0;\n              app.log.info({ returnStage }, "Tarif entretien demandé : nombre d'unités intérieures requis");\n              sendToOpenAI({\n                type: "response.create",\n                response: {\n                  output_modalities: ["audio"],\n                  instructions: 'Dites exactement et uniquement : "Pour vous donner le bon tarif, combien d’unités intérieures avez-vous ?" Aucun mot avant, aucun mot après.',\n                },\n              });\n              return;\n            }\n\n            if (state.tariffFlowMode === "count") {\n              const cleanTariffAnswer = normalizeCleanText(callerMessage);\n              const unsupportedCount = /\\b(six|sept|huit|neuf|dix|[6-9]|[1-9][0-9]+)\\b/.test(cleanTariffAnswer);\n              if (unsupportedCount) {\n                sendTariffResult(null);\n                return;\n              }\n\n              const count = extractIndoorUnitCount(callerMessage);\n              if (!count) {\n                state.tariffRecognitionFailures = Number(state.tariffRecognitionFailures || 0) + 1;\n                if (state.tariffRecognitionFailures >= 2) {\n                  sendTariffResult(null);\n                } else {\n                  sendToOpenAI({\n                    type: "response.create",\n                    response: {\n                      output_modalities: ["audio"],\n                      instructions: 'Dites exactement et uniquement : "Je n’ai pas bien compris le nombre. Combien avez-vous d’unités intérieures ?" Aucun mot avant, aucun mot après.',\n                    },\n                  });\n                }\n                return;\n              }\n\n              state.tariffRecognitionFailures = 0;\n              if (count === 1) {\n                sendTariffResult(getClimMaintenanceQuote({ type: "mono", count: 1 }));\n                return;\n              }\n\n              state.tariffUnitCount = count;\n              state.tariffFlowMode = "grouping";\n              sendToOpenAI({\n                type: "response.create",\n                response: {\n                  output_modalities: ["audio"],\n                  instructions: 'Dites exactement et uniquement : "Ces unités intérieures sont-elles toutes reliées au même groupe extérieur ?" Aucun mot avant, aucun mot après.',\n                },\n              });\n              return;\n            }\n\n            if (state.tariffFlowMode === "grouping") {\n              const cleanGroupingAnswer = normalizeCleanText(callerMessage);\n              const yesNo = extractYesNoClean(callerMessage);\n              const sameGroup = yesNo === "yes" || /\\b(un seul|meme|le meme) groupe exterieur\\b/.test(cleanGroupingAnswer) || /\\b(multi split|multisplit)\\b/.test(cleanGroupingAnswer);\n              const separateGroups = yesNo === "no" || /\\b(chacune|chaque).*\\bgroupe\\b/.test(cleanGroupingAnswer) || /\\b(plusieurs groupes|groupes exterieurs|un groupe par)\\b/.test(cleanGroupingAnswer) || /\\b(mono split|monosplit|monosplits)\\b/.test(cleanGroupingAnswer);\n\n              if (!sameGroup && !separateGroups) {\n                state.tariffRecognitionFailures = Number(state.tariffRecognitionFailures || 0) + 1;\n                if (state.tariffRecognitionFailures >= 2) {\n                  sendTariffResult(null);\n                } else {\n                  sendToOpenAI({\n                    type: "response.create",\n                    response: {\n                      output_modalities: ["audio"],\n                      instructions: 'Dites exactement et uniquement : "Je préfère vérifier : toutes les unités sont sur un seul groupe extérieur, oui ou non ?" Aucun mot avant, aucun mot après.',\n                    },\n                  });\n                }\n                return;\n              }\n\n              const quote = getClimMaintenanceQuote({\n                type: sameGroup && !separateGroups ? "multi" : "mono",\n                count: state.tariffUnitCount,\n              });\n              sendTariffResult(quote);\n              return;\n            }\n          }\n\n          const cleanAdministrativeStages = new Set(["customer_status", "identity", "city", "address", "callback", "callback_number", "qualification"]);`,
    "tarif entretien disponible à tout moment sans clôture prématurée",
  ),
);

patches.push(
  patch(
    `    app.log.info(\n      { customerStatus },\n      "Statut client confirmé par l'appelant"\n    );\n  } else {`,
    `    app.log.info(\n      { customerStatus },\n      "Statut client confirmé par l'appelant"\n    );\n\n    // Une réponse de statut déjà comprise est définitivement consommée par cette étape.\n    // Elle ne doit jamais tomber dans les filtres de petits fragments ni être relue comme une identité.\n    app.log.info({ customerStatus }, "Statut reconnu verrouillé : passage direct à l'étape suivante");\n    requestConversationResponse("customer-status-confirmed");\n    return;\n  } else {`,
    "oui non de statut consommé avant tout filtre de fragment",
  ),
);

export const CLEAN_V1_PRICING_PATCHES = CLEAN_V1_ROBUST_PATCHES + patches.join("\n\n") + "\n\n";
