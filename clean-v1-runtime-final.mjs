import { CLEAN_V1_PATCHES } from "./clean-v1-runtime-patches.mjs";

function finalPatch(search, replacement, label) {
  return `replaceOnce(${JSON.stringify(search)}, ${JSON.stringify(replacement)}, ${JSON.stringify(label)});`;
}

const looseAnchor = `  if (state.flowStage === "qualification" &&\n  assistantText.includes("?")\n) {\n  state.qualificationQuestionCount += 1;`;

const stableServerAnchor = `       if (\n  state.flowStage === "qualification" &&\n  assistantText.includes("?")\n) {\n  state.qualificationQuestionCount += 1;`;

const encodedLooseAnchor = JSON.stringify(looseAnchor);
const encodedStableServerAnchor = JSON.stringify(stableServerAnchor);

if (!CLEAN_V1_PATCHES.includes(encodedLooseAnchor)) {
  throw new Error("Tom V1 propre : ancre qualification à corriger introuvable");
}

const qualificationFixedPatches = CLEAN_V1_PATCHES.replace(
  encodedLooseAnchor,
  encodedStableServerAnchor,
);

const strictInstructionAnchor = `const responseInstructions = strictFlowStages.has(flowLock?.stage)\n  ? [recoveryOverride || flowLock?.instructions, identityGuard].filter(Boolean).join("\\n\\n")\n  : [recoveryOverride || flowLock?.instructions, identityGuard, qualificationBrevity, TOM_CONVERSATION_GUIDANCE].filter(Boolean).join("\\n\\n");`;

const strictInstructionReplacement = `const fixedAdministrativeInstruction = (() => {\n  if (recoveryOverride) return recoveryOverride;\n  if (flowLock?.stage === "customer-status") return 'Dites exactement et uniquement : "Est-ce que vous êtes déjà client chez P C Froid ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "existing-identity") return 'Dites exactement et uniquement : "À quel nom est le dossier ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "new-identity") return 'Dites exactement et uniquement : "Pouvez-vous me donner votre prénom et votre nom, s’il vous plaît ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "new-city") return state.awaitingPostalCode\n    ? 'Dites exactement et uniquement : "Quel est le code postal de l’installation ?" Aucun mot avant, aucun mot après.'\n    : 'Dites exactement et uniquement : "Dans quelle ville se trouve l’installation ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "address") return state.knownCustomerAddress && !state.interventionAddress\n    ? 'Dites exactement et uniquement : "Est-ce que l’intervention est à la même adresse que d’habitude ?" Aucun mot avant, aucun mot après.'\n    : 'Dites exactement et uniquement : "Quelle est l’adresse d’intervention ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "callback") return 'Dites exactement et uniquement : "On peut vous rappeler sur le numéro avec lequel vous appelez ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "callback-number") return 'Dites exactement et uniquement : "Quel numéro je note pour vous rappeler ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "final-question") return 'Dites exactement et uniquement : "Est-ce que vous avez une autre question ou quelque chose à ajouter avant que je transmette votre demande ?" Aucun mot avant, aucun mot après.';\n  if (flowLock?.stage === "qualification-climatisation" && state.qualificationQuestionCount === 0) {\n    const qualificationHistory = normalizeCleanText(state.callerMessages.join(" "));\n    if (/\\b(eau|coule|couler|fuite)\\b/.test(qualificationHistory)) {\n      return 'Dites exactement et uniquement : "Est-ce que l’eau vient de l’unité intérieure ?" Aucun mot avant, aucun mot après. Ne demandez pas au client de répéter le motif.';\n    }\n  }\n  if (flowLock?.stage === "closing") return buildReassuringClosingInstructions({ serviceIntent: state.serviceIntent, equipment: state.explicitEquipment });\n  return null;\n})();\n\nif (fixedAdministrativeInstruction) {\n  app.log.info({ flowLockStage: flowLock?.stage }, "Instruction administrative figée");\n}\n\nconst responseInstructions = fixedAdministrativeInstruction || (strictFlowStages.has(flowLock?.stage)\n  ? [flowLock?.instructions, identityGuard].filter(Boolean).join("\\n\\n")\n  : [flowLock?.instructions, identityGuard, qualificationBrevity, TOM_CONVERSATION_GUIDANCE].filter(Boolean).join("\\n\\n"));`;

const identityTurnAnchor = `          if (!state.identityKnown && !earlyCityCapturedWhileIdentity) {\n            let detectedName = null;`;

const identityTurnReplacement = `          if (!state.identityKnown && !earlyCityCapturedWhileIdentity && flowStageAtTurnStart !== "customer_status") {\n            let detectedName = null;`;

const administrativeStageAnchor = `          const cleanAdministrativeStages = new Set(["customer_status", "identity", "city", "address", "callback", "callback_number", "qualification"]);`;

const administrativeStageReplacement = `          const cleanAdministrativeStages = new Set(["customer_status", "identity", "city", "address", "callback", "callback_number", "qualification"]);\n          const cleanTurnText = normalizeCleanText(callerMessage);\n          const rawTurnText = String(callerMessage || "").trim();\n          const simpleHelloTurn = /^(allo|hallo|hello|hola)$/.test(cleanTurnText) || /^[喂餵](?:[?？])?$/.test(rawTurnText);\n          if (simpleHelloTurn) {\n            state.pendingConversationResponse = false;\n            state.recoveryPrompt = null;\n            cancelActiveResponse();\n            app.log.info({ flowStageAtTurnStart, callerMessage }, "Allô simple ou transcription équivalente : étape conservée sans nouvelle question");\n            setTimeout(() => {\n              sendToOpenAI({\n                type: "response.create",\n                response: {\n                  output_modalities: ["audio"],\n                  instructions: 'Dites exactement et uniquement : "Oui, je vous écoute." Aucun mot avant, aucun mot après. Ne changez pas d’étape et ne posez aucune question.',\n                },\n              });\n            }, 80);\n            return;\n          }\n\n          const neutralConfusionTurn =\n            /^(pardon|quoi|hein|huh)$/.test(cleanTurnText) ||\n            (/\\bje (?:ne )?comprends pas\\b/.test(cleanTurnText) && cleanTurnText.split(/\\s+/).length <= 15);\n          if (neutralConfusionTurn) {\n            state.pendingConversationResponse = false;\n            state.recoveryPrompt = null;\n            cancelActiveResponse();\n            app.log.info({ flowStageAtTurnStart, callerMessage }, "Incompréhension simple : étape conservée sans question parasite");\n            setTimeout(() => {\n              sendToOpenAI({\n                type: "response.create",\n                response: {\n                  output_modalities: ["audio"],\n                  instructions: 'Dites exactement et uniquement : "Pas de souci, je vous écoute." Aucun mot avant, aucun mot après. Ne changez pas d’étape et ne posez aucune question.',\n                },\n              });\n            }, 80);\n            return;\n          }\n\n          const correctedStatusAfterStageAdvance =\n            flowStageAtTurnStart !== "customer_status" ? extractCustomerStatusClean(callerMessage) : null;\n          if (\n            correctedStatusAfterStageAdvance &&\n            state.customerStatus &&\n            correctedStatusAfterStageAdvance !== state.customerStatus &&\n            ["identity", "city", "qualification", "address"].includes(flowStageAtTurnStart)\n          ) {\n            const previousCustomerStatus = state.customerStatus;\n            state.customerStatus = correctedStatusAfterStageAdvance;\n            state.awaitingCustomerStatus = false;\n            state.identityKnown = false;\n            state.identityName = null;\n            state.identityFallbackByPhone = false;\n            if (correctedStatusAfterStageAdvance === "new") {\n              state.knownCustomerAddress = null;\n              state.interventionCity = null;\n              state.interventionPostalCode = null;\n              state.awaitingPostalCode = false;\n              state.pendingCityCandidate = null;\n            }\n            setFlowStage("identity", "correction explicite du statut client");\n            app.log.info({ previousCustomerStatus, correctedStatusAfterStageAdvance }, "Correction tardive du statut client appliquée");\n            sendToOpenAI({\n              type: "response.create",\n              response: {\n                output_modalities: ["audio"],\n                instructions: correctedStatusAfterStageAdvance === "new"\n                  ? 'Dites exactement et uniquement : "D’accord. Pouvez-vous me donner votre prénom et votre nom, s’il vous plaît ?" Aucun mot avant, aucun mot après.'\n                  : 'Dites exactement et uniquement : "D’accord. À quel nom est le dossier ?" Aucun mot avant, aucun mot après.',\n              },\n            });\n            return;\n          }\n\n          if (flowStageAtTurnStart === "address" && state.identityFallbackByPhone) {\n            const identityPrefixMatch = rawTurnText.match(/^([^,;]{2,70})[,;]\\s*(.+)$/u);\n            const explicitLateIdentity = identityPrefixMatch ? extractIdentityClean(identityPrefixMatch[1]) : null;\n            if (explicitLateIdentity) {\n              state.identityKnown = true;\n              state.identityName = explicitLateIdentity;\n              state.identityFallbackByPhone = false;\n              app.log.info({ identity: explicitLateIdentity }, "Identité tardive séparée de l'adresse");\n              sendToOpenAI({\n                type: "response.create",\n                response: {\n                  output_modalities: ["audio"],\n                  instructions: 'Dites exactement et uniquement : "Merci. Quelle est l’adresse d’intervention ?" Aucun mot avant, aucun mot après. Ne mélangez pas le nom avec l’adresse.',\n                },\n              });\n              return;\n            }\n          }\n\n          const genericAppointmentWithoutPurpose =\n            flowStageAtTurnStart === "need" &&\n            /\\b(rendez[- ]?vous|rdv)\\b/.test(cleanTurnText) &&\n            !state.serviceIntent;\n          if (genericAppointmentWithoutPurpose) {\n            state.awaitingCustomerStatus = false;\n            setFlowStage("need", "rendez-vous demandé sans motif encore compris");\n            app.log.info({ callerMessage }, "Rendez-vous sans motif : demande de précision avant le statut client");\n            sendToOpenAI({\n              type: "response.create",\n              response: {\n                output_modalities: ["audio"],\n                instructions: 'Dites exactement et uniquement : "Bien sûr. C’est pour quel type d’intervention ?" Aucun mot avant, aucun mot après. Ne demandez pas encore si la personne est cliente.',\n              },\n            });\n            return;\n          }`;

const customerStatusRecoveryAnchor = `  } else {\n    cancelActiveResponse();\n    const recoveryInstruction = nextRecoveryInstruction("customer_status");\n    state.recoveryPrompt = null;\n\n    setTimeout(() => {\n      sendToOpenAI({\n        type: "response.create",\n        response: {\n          output_modalities: ["audio"],\n          instructions: recoveryInstruction,\n        }\n      });\n    }, 80);\n\n    return;\n  }\n}`;

const customerStatusRecoveryReplacement = `  } else {\n    cancelActiveResponse();\n    const previousStatusRecovery = Number(state.recoveryCounts?.customer_status || 0);\n    const statusRecoveryAttempt = previousStatusRecovery + 1;\n    state.recoveryCounts.customer_status = statusRecoveryAttempt;\n    state.recoveryPrompt = null;\n    app.log.info({ field: "customer_status", attempt: statusRecoveryAttempt }, "Relance statut client verrouillée");\n\n    const statusRecoveryInstruction = statusRecoveryAttempt === 1\n      ? 'Dites exactement et uniquement : "Je n’ai pas bien compris. Est-ce que vous êtes déjà client chez PC Froid ?" Aucun mot avant, aucun mot après. Ne demandez ni le nom ni une autre information.'\n      : 'Dites exactement et uniquement : "Dites-moi simplement oui si vous êtes déjà client chez PC Froid, ou non si c’est votre première demande." Aucun mot avant, aucun mot après. Ne demandez ni le nom ni une autre information.';\n\n    setTimeout(() => {\n      sendToOpenAI({\n        type: "response.create",\n        response: {\n          output_modalities: ["audio"],\n          instructions: statusRecoveryInstruction,\n        }\n      });\n    }, 80);\n\n    return;\n  }\n}`;

const hangupMarkAnchor = `            app.log.info("Fin audio confirmée par Twilio ; raccrochage");\n            if (socket.readyState === WebSocket.OPEN) {\n              socket.close(1000, "call-complete");\n            }`;

const hangupMarkReplacement = `            app.log.info("Fin audio confirmée par Twilio ; pause de 3 secondes avant raccrochage");\n            setTimeout(() => {\n              if (socket.readyState === WebSocket.OPEN) {\n                socket.close(1000, "call-complete");\n              }\n            }, 3000);`;

export const CLEAN_V1_FINAL_PATCHES = qualificationFixedPatches +
  finalPatch(
    strictInstructionAnchor,
    strictInstructionReplacement,
    "phrases administratives figées sans improvisation",
  ) + "\n\n" +
  finalPatch(
    identityTurnAnchor,
    identityTurnReplacement,
    "un tour statut client ne peut jamais devenir identité",
  ) + "\n\n" +
  finalPatch(
    administrativeStageAnchor,
    administrativeStageReplacement,
    "allô confusion statut tardif et identité adresse séparés",
  ) + "\n\n" +
  finalPatch(
    customerStatusRecoveryAnchor,
    customerStatusRecoveryReplacement,
    "relance statut client toujours sur le statut",
  ) + "\n\n" +
  finalPatch(
    hangupMarkAnchor,
    hangupMarkReplacement,
    "pause de trois secondes après la dernière phrase",
  ) + "\n\n";
