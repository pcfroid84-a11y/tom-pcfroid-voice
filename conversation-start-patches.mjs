function patch(search, replacement, label) {
  return `replaceOnce(${JSON.stringify(search)}, ${JSON.stringify(replacement)}, ${JSON.stringify(label)});`;
}

const patches = [];

patches.push(
  patch(
    `         const normalizedCallerMessage = normalizeText(callerMessage);`,
    `         const normalizedCallerMessage = normalizeText(callerMessage);\n\nif (\n  (\n    state.explicitEquipment === "climatisation" ||\n    detectedEquipment === "climatisation" ||\n    /\\bclim\\w*\\b/.test(normalizedCallerMessage)\n  ) &&\n  /\\b(entretien|maintenance|nettoy\\w*|desinfect\\w*)\\b/.test(normalizedCallerMessage)\n) {\n  state.serviceIntent = "entretien";\n  app.log.info(\n    { callerMessage, serviceIntent: state.serviceIntent },\n    "Nettoyage climatisation classé comme entretien"\n  );\n}\n\nif (\n  flowStageAtTurnStart === "need" &&\n  /^(oui|non|ok|d accord|d'accord)$/.test(normalizedCallerMessage) &&\n  !detectedEquipment\n) {\n  app.log.info(\n    { callerMessage },\n    "Réponse courte isolée au démarrage : Tom reste en écoute"\n  );\n\n  sendToOpenAI({\n    type: "response.create",\n    response: {\n      output_modalities: ["audio"],\n      instructions:\n        'Répondez exactement et uniquement : "Je vous écoute." Puis taisez-vous et laissez l’appelant expliquer sa demande. Ne posez aucune question.'\n    }\n  });\n\n  return;\n}\n\n// SÉCURITÉ : si l'appelant pose une autre question au lieu de répondre à\n// l'information attendue, Tom répond brièvement puis reprend exactement\n// l'étape en cours. La parenthèse ne doit ni faire avancer le parcours, ni\n// être enregistrée comme nom/ville/adresse, ni compter comme incompréhension.\nconst sideQuestionPending = (() => {\n  if (\n    flowStageAtTurnStart === "need" &&\n    state.serviceIntent === "entretien" &&\n    !state.explicitEquipment\n  ) {\n    return "Sur quel type d’appareil souhaitez-vous un entretien ?";\n  }\n  if (flowStageAtTurnStart === "customer_status") {\n    return "Est-ce que vous êtes déjà client chez P C Froid ?";\n  }\n  if (flowStageAtTurnStart === "identity") {\n    return state.customerStatus === "existing"\n      ? "À quel nom est le dossier ?"\n      : "Pouvez-vous me donner votre prénom et votre nom, s’il vous plaît ?";\n  }\n  if (flowStageAtTurnStart === "city") {\n    return state.awaitingPostalCode\n      ? "Quel est le code postal de l’installation ?"\n      : "Dans quelle ville se trouve l’installation ?";\n  }\n  if (flowStageAtTurnStart === "address") {\n    return state.knownCustomerAddress\n      ? "Est-ce que l’intervention est à la même adresse que d’habitude ?"\n      : "Quelle est l’adresse d’intervention ?";\n  }\n  if (flowStageAtTurnStart === "callback") {\n    return "On peut vous rappeler sur le numéro avec lequel vous appelez ?";\n  }\n  if (flowStageAtTurnStart === "callback_number") {\n    return "Quel numéro je note pour vous rappeler ?";\n  }\n  if (flowStageAtTurnStart === "qualification") {\n    return "__REPRENDRE_DERNIERE_QUESTION__";\n  }\n  return null;\n})();\n\nconst looksLikeSideQuestion = Boolean(sideQuestionPending) && (\n  /[?？]/u.test(callerMessage) ||\n  /\\b(est ce que|est-ce que|combien|pourquoi|comment|quel tarif|quelle tarif|quel prix|quelle prix|c est combien|c'est combien|ca coute|ça coute|ça coûte|vous faites|vous pouvez|vous intervenez|vous avez|vous prenez|vous connaissez|vous travaillez|vous etes ouvert|vous êtes ouvert|quand est ce|quand est-ce|quel delai|quel délai|garantie|tarif|prix)\\b/.test(normalizedCallerMessage)\n);\n\nlet sideQuestionAlsoAnswersPendingField = false;\n\nif (looksLikeSideQuestion) {\n  if (flowStageAtTurnStart === "customer_status") {\n    sideQuestionAlsoAnswersPendingField = Boolean(\n      extractCustomerStatusAnswer(callerMessage)\n    );\n  } else if (flowStageAtTurnStart === "identity") {\n    sideQuestionAlsoAnswersPendingField = Boolean(\n      extractDirectIdentityAnswer(callerMessage)\n    );\n  } else if (flowStageAtTurnStart === "city") {\n    sideQuestionAlsoAnswersPendingField = state.awaitingPostalCode\n      ? Boolean(extractPostalCode(callerMessage))\n      : Boolean(extractCityCandidate(callerMessage, true));\n  } else if (flowStageAtTurnStart === "address") {\n    const normalizedSideAddress = normalizeText(callerMessage);\n    sideQuestionAlsoAnswersPendingField =\n      /\\d/.test(callerMessage) ||\n      /\\b(rue|avenue|av|boulevard|bd|chemin|route|impasse|allee|allée|place|lotissement|residence|résidence|zone|quartier)\\b/i.test(callerMessage) ||\n      (Boolean(state.knownCustomerAddress) && /^(oui|non)\\b/.test(normalizedSideAddress));\n  } else if (flowStageAtTurnStart === "callback") {\n    sideQuestionAlsoAnswersPendingField =\n      /^(oui|non)\\b/.test(normalizedCallerMessage) ||\n      /\\b(ce numero|ce numéro|celui qui s affiche|celui qui s'affiche)\\b/.test(normalizedCallerMessage);\n  } else if (flowStageAtTurnStart === "callback_number") {\n    const digits = String(callerMessage || "").replace(/\\D/g, "");\n    sideQuestionAlsoAnswersPendingField = digits.length >= 8;\n  } else if (flowStageAtTurnStart === "qualification") {\n    // Exemple : « 5 ans, c'est grave ? » contient aussi une vraie réponse.\n    // On laisse alors le parcours normal exploiter l'information.\n    sideQuestionAlsoAnswersPendingField =\n      /\\b(oui|non|[0-9]+|un|deux|trois|quatre|cinq|six|sept|huit|neuf|dix|an|ans|mois|semaine|semaines|jour|jours|demarre|démarre|fonctionne|voyant|code)\\b/.test(normalizedCallerMessage);\n  }\n}\n\nif (\n  looksLikeSideQuestion &&\n  !sideQuestionAlsoAnswersPendingField &&\n  !state.partnerOrSupplierFlow &&\n  !callerIsClosing(callerMessage)\n) {\n  app.log.info(\n    { callerMessage, flowStage: flowStageAtTurnStart, pendingQuestion: sideQuestionPending },\n    "Question latérale : Tom répond puis reprend l'étape sans la perdre"\n  );\n\n  const resumeInstruction = sideQuestionPending === "__REPRENDRE_DERNIERE_QUESTION__"\n    ? "Ensuite, reposez exactement la dernière question de qualification que vous aviez posée juste avant, sans en inventer une nouvelle."\n    : 'Ensuite, revenez naturellement à la question en attente et posez exactement : "' + sideQuestionPending + '"';\n\n  sendToOpenAI({\n    type: "response.create",\n    response: {\n      output_modalities: ["audio"],\n      instructions:\n        "L’appelant vient de poser une autre question au lieu de répondre à l’information attendue. Répondez d’abord à sa question en UNE phrase courte et naturelle, uniquement avec les informations PC Froid fiables déjà présentes dans votre contexte. Si vous ne connaissez pas la réponse ou si le tarif/délai n’est pas validé, dites simplement que l’équipe pourra le confirmer, sans inventer. " +\n        resumeInstruction +\n        " Maximum deux phrases au total. Ne changez pas d’étape, ne considérez pas sa question comme la réponse attendue et ne posez aucune autre question."\n    }\n  });\n\n  return;\n}`,
    "nettoyage clim = entretien + questions latérales sécurisées",
  ),
);

patches.push(
  patch(
    `'Respectez impérativement le parcours PC Froid. Si le client demande explicitement si PC Froid réalise ce service, répondez clairement en une phrase courte, sans inventer. Sinon, ne reformulez pas le symptôme. Ne posez aucune question technique. La seule question autorisée ensuite est exactement : "Est-ce que vous êtes déjà client chez P C Froid ?" Puis arrêtez-vous et attendez la réponse.'`,
    `'Dites exactement et uniquement : "Est-ce que vous êtes déjà client chez P C Froid ?" Puis arrêtez-vous et attendez la réponse. Ne prononcez aucune consigne interne et n’ajoutez aucune phrase avant.'`,
    "question statut client sans lecture des consignes internes",
  ),
);

patches.push(
  patch(
    `const responseInstructions = [\n  recoveryOverride || flowLock?.instructions,\n  identityGuard,\n  TOM_CONVERSATION_GUIDANCE,\n]\n  .filter(Boolean)\n  .join("\\n\\n");`,
    `const qualificationBrevityGuard =\n  state.flowStage === "qualification"\n    ? "QUALIFICATION : réponse très courte. Maximum 25 mots au total. Une seule information utile puis une seule question. Aucun long commentaire, aucune explication développée, aucune reformulation du dossier."\n    : null;\n\nconst responseInstructions = [\n  recoveryOverride || flowLock?.instructions,\n  identityGuard,\n  qualificationBrevityGuard,\n  TOM_CONVERSATION_GUIDANCE,\n]\n  .filter(Boolean)\n  .join("\\n\\n");`,
    "qualification limitée à une réponse courte",
  ),
);

export const CONVERSATION_START_PATCHES = patches.join("\n\n") + "\n\n";
